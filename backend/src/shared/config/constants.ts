export const RESERVATION_EXPIRATION_PROVIDER_MILLIS = 15 * 60 * 1000;
export const RESERVATION_EXPIRATION_INTERNAL_MILLIS = 20 * 60 * 1000; // 5 min mäs para esperar webhook
export const SWEEP_INTERVAL_MILLIS = 5 * 60 * 1000;
export const DONATION_EXPIRY_INTERVAL_MILLIS = 20 * 60 * 1000;
